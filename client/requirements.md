## Packages
recharts | Visualization of EDA stats and model metrics
framer-motion | Smooth transitions between wizard steps
clsx | Utility for conditional classes (standard in shadcn)
tailwind-merge | Utility for merging tailwind classes (standard in shadcn)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
